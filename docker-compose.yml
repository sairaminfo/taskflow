services:
  taskflow:
    build: .
    ports:
      - "8080:8080"
    environment:
      TASKFLOW_ADDR: ":8080"
      TASKFLOW_SHUTDOWN_TIMEOUT: "10s"
      TASKFLOW_EVENT_BUFFER: "256"
      TASKFLOW_EVENT_WORKERS: "4"
    restart: unless-stopped
